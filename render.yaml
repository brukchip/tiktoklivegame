services:
  - type: web
    name: tiktok-live-game
    runtime: node
    buildCommand: npm install
    startCommand: node backend-server.js
    envVars:
      - key: NODE_VERSION
        value: 25.2.1
